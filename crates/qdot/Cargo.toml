[package]
name = "bloomery-qdot"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT"

[lib]
name = "qdot"

[lints]
workspace = true

[dependencies]
bloomery-gguf = { path = "../gguf" }
