[package]
name = "q3k-gemv"
version = "0.1.0"
edition = "2024"
publish = false   # 발행하지 않는다. cargo deny의 licenses.private가 이걸 본다.

[lints]
workspace = true

[dependencies]
cuda-device.workspace = true
cuda-host.workspace = true
# Shared host-side runtime, published from NVlabs/cutile-rs.
cuda-core.workspace = true
