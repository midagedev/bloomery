[package]
name = "bloomery-q3k-gemv"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT"

[[bin]]
name = "q3k-gemv"
path = "src/main.rs"

[lints]
workspace = true

[dependencies]
cuda-device.workspace = true
cuda-host.workspace = true
# Shared host-side runtime, published from NVlabs/cutile-rs.
cuda-core.workspace = true
