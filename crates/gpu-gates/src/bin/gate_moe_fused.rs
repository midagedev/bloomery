//! Gate for the MoE fused kernels (`crates/gpu/src/moe_fused.rs`): the
//! per-op path and the fused path, bit-identical, on layer 1 of the dump.
//! Stub until the round lands: exits 2.
fn main() {
    eprintln!("gate_moe_fused: not implemented yet");
    std::process::exit(2);
}
