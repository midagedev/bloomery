//! Gate for the assembled decode step (`GpuModel::step`), block 0 first:
//! the engine's own taps against the dump. Stub until the round lands:
//! exits 2.
fn main() {
    eprintln!("gate_p8: not implemented yet");
    std::process::exit(2);
}
