[package]
name = "bloomery-engram"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT"

[lib]
name = "engram"

[lints]
workspace = true

[dependencies]
bloomery-gguf = { path = "../gguf" }
libc.workspace = true
memmap2.workspace = true
thiserror.workspace = true

[dev-dependencies]
# Integration tests do not inherit the crate's own dependencies, and the
# layout gate re-derives the geometry from the header itself.
bloomery-gguf = { path = "../gguf" }
